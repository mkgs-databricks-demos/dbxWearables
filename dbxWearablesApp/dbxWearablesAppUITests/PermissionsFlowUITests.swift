import XCTest

final class PermissionsFlowUITests: XCTestCase {

    // TODO: Add UI tests for the HealthKit permissions flow.
    // Note: HealthKit authorization dialogs are system-level and cannot be
    // directly controlled in UI tests. Test the surrounding navigation flow.
}
