# Resources — Asset Catalog

Contains the Xcode asset catalog for the app.

## Contents

| Asset | Description |
|---|---|
| `Assets.xcassets/` | Xcode asset catalog. Currently contains the `AccentColor` color set, set to Databricks red (`#FF3621`). This color is used as the system accent color throughout the app (tab bar tint, default button color, navigation highlights). |
