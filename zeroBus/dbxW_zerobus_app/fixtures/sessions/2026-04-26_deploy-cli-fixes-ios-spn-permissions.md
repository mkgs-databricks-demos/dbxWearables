## Session: deploy.sh CLI Fixes, iOS SPN Permissions & Deploy Readiness

**Date:** 2026-04-26
**Bundle:** dbxW_zerobus_app + dbxW_zerobus_infra + deploy.sh
**Focus:** End-to-end deploy.sh fixes, iOS Bootstrap SPN declarative permissions, target-specific Lakebase project IDs, route guard verification, assistant instructions streamlining

---

### Context

After the 2026-04-25 JWT auth session, the infra bundle had been deployed and UC setup job run successfully — both SPNs created, all 9 secret scope keys provisioned. The first full `deploy.sh --target dev` run exposed a chain of CLI compatibility and file-upload issues that blocked the app bundle deployment.

---

### Problems & Root Causes

#### 1. Secrets List JSON Parsing Failure

**Error:** `Could not parse secrets list from scope 'dbxw_zerobus_credentials'`

**Root cause (two issues):**
- `2>&1` on the `list-secrets` command merged stderr warnings into the JSON output, corrupting it
- The Python parser expected `{"secrets": [...]}` (old CLI format), but the current CLI returns a flat array `[{...}]`

**Fix (deploy.sh):**
- Changed `2>&1` → `2>/dev/null` on the `list-secrets` line
- Updated the Python parser to handle both JSON formats: `items = data.get('secrets', data) if isinstance(data, dict) else data`

#### 2. Lakebase Project Status Warning

**Error:** `Lakebase project 'dbxw-zerobus-wearables' not found or not accessible`

**Root cause:** `databricks postgres get-project` requires the full resource path `projects/{project_id}`, but deploy.sh was passing just the bare project ID. The `list-endpoints` call on L360 already had the `projects/` prefix — inconsistency.

**Fix (deploy.sh):**
- Added `projects/` prefix to the `get-project` call: `"projects/${project_id}"`
- Changed `2>&1` → `2>/dev/null` on both Lakebase CLI commands

#### 3. Ghost `.bashrc` Upload Error

**Error:** `Error: open fixtures/.bashrc: no such file or directory`

**Root cause:** The `databricks bundle deploy` CLI (running from the web terminal) tries to upload a `fixtures/.bashrc` file that does not exist in the workspace API, FUSE filesystem, or sync snapshot. The file is a phantom entry visible only to the web terminal's FUSE mount — likely a Linux shell artifact. The workspace UI "Deploy" button (which uses server-side APIs) is unaffected.

**Fix:**
- Created `.databricksignore` with patterns: `.bashrc`, `**/.bashrc`, `fixtures/.bashrc`
- Added `.bashrc` to `.gitignore` as well (CLI reads both)

#### 4. Missing iOS SPN Permissions in App Bundle

**Error:** No error — but the iOS Bootstrap SPN lacked `CAN_USE` on the app resource.

**Root cause:** The `zerobus_ingest.app.yml` permissions blocks didn't include the iOS SPN. The SPN's `application_id` is dynamically generated by the infra UC setup job, so it can't be hardcoded.

**Fix (three files):**
- `databricks.yml` (app): Added `ios_bootstrap_spn_id` variable with `default: ""` (deploy.sh overrides via `--var`)
- `zerobus_ingest.app.yml`: Added `- service_principal_name: ${var.ios_bootstrap_spn_id}` / `level: CAN_USE` to all 4 permissions blocks (base, dev, hls_fde, prod)
- `deploy.sh`: Added `read_ios_spn_id()` function that reads the SPN application_id from the secret scope and passes it as `--var ios_bootstrap_spn_id=<value>` to app bundle deploy

#### 5. Shared Lakebase Project ID Across Targets

**Problem:** dev and hls_fde targets share the same workspace but used the same hardcoded Lakebase project ID `dbxw-zerobus-wearables`. When hls_fde deploys, it would collide with dev.

**Fix (infra bundle, following scAuditor pattern):**
- `databricks.yml` (infra): Added `lakebase_project_id` variable with default `dbxw-zerobus-wearables`; hls_fde overrides to `dbxw-zerobus-wearables-hls-fde`
- `wearables.lakebase.yml`: Changed `project_id: dbxw-zerobus-wearables` → `project_id: ${var.lakebase_project_id}`

#### 6. iOS Bootstrap SPN Client Secret Not Checked Before Deploy

**Problem:** The iOS SPN client secret wasn't in the deploy readiness checks. If an admin forgot to provision it, the app would deploy but iOS auth would silently fail.

**Fix (three files):**
- `databricks.yml` (both bundles): Added `ios_bootstrap_client_secret_key` variable, schema-qualified per target (e.g., `ios_bootstrap_client_secret_dev_matthew_giglia_wearables`)
- `deploy.sh`: Added `IOS_BOOTSTRAP_CLIENT_SECRET_KEY` to `ADMIN_PROVISIONED_KEYS` array, Python parser to resolve from bundle summary, and `ok()` status line

---

### Deploy Progression

| Attempt | Result | Blocker |
| --- | --- | --- |
| 1 | Infra deployed, app blocked | Secrets list JSON parse failure |
| 2 | Secrets verified, app validated | `fixtures/.bashrc` ghost file upload error |
| 3 | App deployed via workspace UI | deploy.sh still hit `.bashrc` (web terminal only) |
| 4 | Full `deploy.sh --target dev` success | All fixes in place |

---

### OTel Log Analysis — Phase 1 Route Guard Confirmed Active

Queried `hls_fde_dev.dev_matthew_giglia_wearables.dbxw_0bus_ingest_otel_logs` (last 3 days):

| Log Pattern | Count | Meaning |
| --- | --- | --- |
| `403 proxy-unverified denied access to admin zone: POST /v1/healthkit/ingest` | 1,563 | Unknown SPNs blocked from ingest |
| `403 ios-spn denied access to admin zone: POST /v1/healthkit/ingest` | 185 | iOS SPN correctly identified but denied ingest (auth + health only) |
| `PayloadTooLargeError` | 1,294 | Workspace-user sessions (browser load tests) hitting Express body limit |
| `[ZeroBus] Ingest failed: Failed to create stream` | 5,651 | Stream creation errors (separate issue) |

**Verdict:** Phase 1 auth enforcement is active. Both `proxy-unverified` and `ios-spn` callers receive 403 on ingest routes, matching the `spn-route-guard.ts` access matrix.

**Note:** Ingest route is classified as `admin` zone in the logs, not `ingest` zone — the zone pattern matching in `spn-route-guard.ts` may need review (functional behavior is correct, just the log label).

---

### Assistant Instructions Streamlined

Reduced `.assistant_instructions.md` from 26,162 chars / 477 lines / 20 sections to 9,753 chars / 216 lines / 14 sections (63% reduction). Key changes:
- 8 brand skill sections → 1 index table (detail lives in skill files)
- Cross-project conventions consolidated
- Project contexts compacted (preserved all IDs, paths, conventions)
- 43/43 critical fact verification checks passed

---

### Admin Steps Completed

- ZeroBus SPN OAuth client secret generated and stored as `client_secret_dev_matthew_giglia_wearables` in scope `dbxw_zerobus_credentials` — app can now authenticate to workspace API at runtime

---

### Files Modified

| File | Changes | Bundle |
| --- | --- | --- |
| `deploy.sh` | 7 fixes: `2>&1`→`2>/dev/null` on list-secrets + Lakebase commands, JSON parser for both formats, `projects/` prefix on get-project, `read_ios_spn_id()` + `--var` passthrough, `IOS_BOOTSTRAP_CLIENT_SECRET_KEY` in admin checks + parser + status | root |
| `dbxW_zerobus_app/databricks.yml` | Added `ios_bootstrap_spn_id` variable, `ios_bootstrap_client_secret_key` variable + per-target overrides | app |
| `dbxW_zerobus_app/resources/zerobus_ingest.app.yml` | Added iOS SPN `CAN_USE` to 4 permissions blocks | app |
| `dbxW_zerobus_app/.databricksignore` | Created — excludes `.bashrc` ghost file | app |
| `dbxW_zerobus_app/.gitignore` | Added `.bashrc` exclusion | app |
| `dbxW_zerobus_infra/databricks.yml` | Added `lakebase_project_id` variable + hls_fde override, `ios_bootstrap_client_secret_key` variable + per-target overrides | infra |
| `dbxW_zerobus_infra/resources/wearables.lakebase.yml` | `project_id` → `${var.lakebase_project_id}` | infra |
| `.assistant_instructions.md` | Streamlined from 26K → 10K chars (63% reduction) | user home |

---

### Design Decisions

| Decision | Rationale |
| --- | --- |
| `default: ""` for `ios_bootstrap_spn_id` | Allows standalone `bundle validate` without deploy.sh; empty string is harmless in permissions (no grant created) |
| deploy.sh reads SPN ID from scope, passes via `--var` | Keeps permissions declarative in YAML; survives bundle re-deploys without drift |
| Three `.bashrc` patterns in `.databricksignore` | Belt-and-suspenders — bare name, glob, and explicit path covers all CLI globbing implementations |
| Target-specific Lakebase project IDs | Prevents collisions when dev + hls_fde share the same workspace (same pattern as scAuditor) |
| `2>/dev/null` instead of `2>&1` on CLI JSON commands | Prevents stderr warnings from corrupting JSON piped to Python parsers |
| Schema-qualified `ios_bootstrap_client_secret_key` | Same pattern as `client_id_dbs_key` / `client_secret_dbs_key` — avoids key collisions in shared scope |

---

### Phase & Task Roadmap

#### Phase 1 — Auth Infrastructure (COMPLETE)

All server-side auth infrastructure is deployed and verified:
- `auth-service.ts` — Apple JWKS validation, HS256 signing, refresh rotation, Lakebase migration
- `auth-routes.ts` — `/apple`, `/refresh`, `/revoke`, `/health`
- `jwt-auth.ts` — `requireAuth` + `optionalAuth` middleware
- `spn-route-guard.ts` — Caller identification, access matrix (verified via OTel logs)
- `rate-limit.ts` — Sliding window counters
- `jose` dependency `^5.0.0` in package.json
- Lakebase schema — auto-migrates on startup (auth.users, auth.devices, auth.refresh_tokens)
- Both SPNs created, all secret scope keys provisioned
- deploy.sh fully working with all readiness checks

#### Phase 2 — Connect Auth to Ingest (NOT STARTED)

**Goal:** Wire JWT authentication into the data path so bronze records carry real user identity.

| Step | Task | Files |
| --- | --- | --- |
| 2.1 | **Create shared `extractUser()` utility** — extracts identity from `x-forwarded-email` (AppKit proxy header) or JWT `req.auth?.sub` (Lakebase user_id UUID) | `server/utils/extract-user.ts` (new) |
| 2.2 | **Refactor `ingest-routes.ts`** — replace local `extractUserFromToken()` with shared import | `server/routes/zerobus/ingest-routes.ts` |
| 2.3 | **Wire `optionalAuth` middleware** into `ingest-routes.ts` — validates JWT if present, continues if absent | `server/routes/zerobus/ingest-routes.ts` |
| 2.4 | **Update `extract-user.ts`** — read `req.auth?.sub` when JWT is valid, fall back to `x-forwarded-email`, then `'anonymous'` | `server/utils/extract-user.ts` |
| 2.5 | **Wire into `load-test-routes.ts`** — use shared `extractUser()` so synthetic records carry real user identity instead of hardcoded `'synthetic-load-test'` | `server/routes/testing/load-test-routes.ts` |
| 2.6 | **Remove `userId` from client POST body** in `LoadTestPage.tsx` — identity is a server concern | `client/src/pages/testing/LoadTestPage.tsx` |

**Current bug:** All ~6M+ synthetic records in the bronze table have `user_id = 'synthetic-load-test'` instead of the real workspace user. Phase 2 fixes this.

**Note on zone classification:** The route guard logs show `POST /v1/healthkit/ingest` matching the `admin` zone instead of `ingest` zone. Review zone patterns in `spn-route-guard.ts` (functional behavior is correct).

#### Phase 3 — Load Test History (NOT STARTED)

**Goal:** Persist structured load test metadata to Lakebase with automatic Lakehouse Sync to UC Delta tables.

**Full implementation plan:** See `fixtures/Load Test History Implementation Plan` notebook (Cell 2) for complete architecture, schema design, API endpoints, and 10-step implementation order.

| Step | Task | Files |
| --- | --- | --- |
| 3.1 | Create `load-test-history-service.ts` — Lakebase schema + CRUD for `app.load_test_runs` and `app.load_test_type_results`. Includes `ALTER TABLE ... REPLICA IDENTITY FULL` for Lakehouse Sync | `server/services/load-test-history-service.ts` (new) |
| 3.2 | Add `presetLabel` to `LoadTestRequest` interface and POST body | `load-test-routes.ts`, `LoadTestPage.tsx` |
| 3.3 | Wire history writes into SSE streaming flow — INSERT on start, UPDATE on progress/complete/error | `load-test-routes.ts` |
| 3.4 | Add `GET /api/v1/testing/history` + `GET .../history/:runId` + `DELETE .../history/:runId` endpoints | `load-test-routes.ts` |
| 3.5 | Add current-state view DDL to infra bundle's `target-table-ddl` notebook (runs after Lakehouse Sync creates history tables) | `dbxW_zerobus_infra/src/uc_setup/target-table-ddl` (ID: `2003193682514586`) |
| 3.6 | Enable Lakehouse Sync in Lakebase UI: source schema `app` → destination `${var.catalog}.${var.schema}` | Lakebase UI (one-time manual step per target) |
| 3.7 | Build `LoadTestHistory` React component — timeline table + charts (throughput, records by type, pool scaling, duration by preset) | `client/src/pages/testing/LoadTestHistory.tsx` (new) |
| 3.8 | Integrate history component into `LoadTestPage.tsx` | `LoadTestPage.tsx` |
| 3.9 | End-to-end test: run test → verify Lakebase → verify `lb_*_history` Delta → verify views → verify UI | Manual |

**Architecture:** Single write to Lakebase → Lakehouse Sync (CDC via wal2delta) replicates to UC Delta tables automatically. No dual-write. Current-state views deduplicate SCD Type 2 history.

**Schema:** `app.load_test_runs` (run metadata, config, results, pool state) + `app.load_test_type_results` (per-type breakdown per run). See implementation plan notebook for full DDL.

#### Phase 4 — iOS App Auth Implementation (NOT STARTED)

**Goal:** Implement Sign in with Apple and JWT token management in the iOS HealthKit app.

| Step | Task |
| --- | --- |
| 4.1 | Embed iOS Bootstrap SPN credentials (client_id + client_secret) in binary via AppKit proxy auth |
| 4.2 | Implement `ASAuthorizationAppleIDProvider` (Sign in with Apple) |
| 4.3 | Build auth API client — POST to `/api/v1/auth/apple` with identity_token + device_id |
| 4.4 | Expand `KeychainHelper` — store access_token, refresh_token, SPN creds separately |
| 4.5 | Implement token refresh — POST to `/api/v1/auth/refresh` (15 min access TTL) |
| 4.6 | Add 401 retry interceptor — catch TOKEN_EXPIRED, refresh, retry |
| 4.7 | Verify bundle ID match — must be `com.dbxwearables.healthKit` |
| 4.8 | Implement revoke on logout — POST to `/api/v1/auth/revoke` |

**Already working in iOS app:** `DeviceIdentifier.current`, `APIService` Bearer header, `APIConfiguration`

#### Future — hls_fde Target Deployment

When deploying hls_fde:
1. Infra bundle creates new Lakebase project `dbxw-zerobus-wearables-hls-fde`
2. Update app `databricks.yml` hls_fde target: `postgres_branch` and `postgres_database` to new project + database ID
3. Store `ios_bootstrap_client_secret_wearables` in the scope
4. Enable Lakehouse Sync for the new project

---

### Secret Scope State (dbxw_zerobus_credentials)

All 10 expected keys present after this session:

| Key | Status | Provisioner |
| --- | --- | --- |
| `client_id_dev_matthew_giglia_wearables` | ✓ | UC setup job |
| `client_secret_dev_matthew_giglia_wearables` | ✓ | Admin (done this session) |
| `workspace_url` | ✓ | UC setup job |
| `zerobus_endpoint` | ✓ | UC setup job |
| `target_table_name` | ✓ | UC setup job |
| `zerobus_stream_pool_size` | ✓ | UC setup job |
| `jwt_signing_secret` | ✓ | UC setup job (auto-generated) |
| `apple_bundle_id` | ✓ | UC setup job |
| `ios_spn_application_id` | ✓ | UC setup job |
| `ios_bootstrap_client_secret_dev_matthew_giglia_wearables` | **Pending** | Admin (generate + store) |
